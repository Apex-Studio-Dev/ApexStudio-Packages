diff --git a/build-package.sh b/build-package.sh
--- a/build-package.sh
+++ b/build-package.sh
@@ -664,6 +664,10 @@ if [[ "${TERMUX_INSTALL_DEPS-false}" == "true" || "${TERMUX_PACKAGE_LIBRARY-bio
 		gpg --no-tty --command-file <(echo -e "trust\n5\ny") --edit-key 998DE27318E867EA976BA877389CEED64573DFCA
 	}
+	gpg --list-keys 5F128F230DEEF535 > /dev/null 2>&1 || {
+		gpg --import "$TERMUX_SCRIPTDIR/packages/termux-keyring/aurastudio.gpg"
+		gpg --no-tty --command-file <(echo -e "trust\n5\ny") --edit-key 5F128F230DEEF535
+	}
 fi
 
 for (( i=0; i < ${#PACKAGE_LIST[@]}; i++ )); do
